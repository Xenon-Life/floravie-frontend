# Copy to .env to override (optional — .env.development sets defaults for dev)
#
# Dev with Vite proxy (backend on port 3000):
VITE_BACKEND_URL=/api
#
# Or call the API directly (requires backend CORS / FRONTEND_URL):
# VITE_BACKEND_URL=http://localhost:3000/api
